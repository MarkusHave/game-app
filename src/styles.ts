import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
  root: {
    alignContent: 'center',
    backgroundColor: 'grey',
  },
  card: {
    margin: 10,
  },
  media: {
    height: 0,
    paddingTop: '56.25%',
  },
});
